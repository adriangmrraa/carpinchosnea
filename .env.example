# Variables de entorno para producción en Render
# Copia este archivo a .env y configura las variables

# URL base de la API (para el cliente en producción)
VITE_API_BASE=https://tu-app-en-render.onrender.com/api

# Puerto del servidor (Render lo asigna automáticamente)
PORT=10000